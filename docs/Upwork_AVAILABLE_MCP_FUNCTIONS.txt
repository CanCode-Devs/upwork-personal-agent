ALL TOOLS
  upwork__boost_profile
  upwork__confirm_attachment_upload
  upwork__confirm_draft
  upwork__find_jobs
  upwork__find_saved_jobs
  upwork__get_account
  upwork__get_agency
  upwork__get_agency_dashboard
  upwork__get_draft
  upwork__get_freelancer_dashboard
  upwork__get_freelancer_financials
  upwork__get_messages
  upwork__get_profile
  upwork__get_tool_help
  upwork__get_upload_status
  upwork__list_accounts
  upwork__list_contracts
  upwork__list_freelancer_proposals
  upwork__list_milestones
  upwork__list_offers
  upwork__manage_proposals
  upwork__respond_to_offer
  upwork__save_job
  upwork__send_message
  upwork__set_tool_mode
  upwork__set_tool_permission
  upwork__start_attachment_upload
  upwork__store_uploaded_files
  upwork__submit_milestones
  upwork__update_draft
  upwork__update_profile

INTERESTING ['upwork__boost_profile', 'upwork__find_jobs', 'upwork__find_saved_jobs', 'upwork__get_account', 'upwork__get_agency_dashboard', 'upwork__get_freelancer_dashboard', 'upwork__list_accounts', 'upwork__list_freelancer_proposals', 'upwork__list_offers', 'upwork__manage_proposals', 'upwork__respond_to_offer', 'upwork__save_job', 'upwork__submit_milestones']

=== SCHEMA upwork__boost_profile ===
{"org_uid": {"description": "Your Upwork org_uid from list_accounts", "type": "string"}, "params": {"description": "Action-specific parameters (see allOf branches per action when present)", "type": "object"}, "action": {"description": "The operation to perform", "enum": ["get_status", "toggle_availability_badge", "get_suggested_boost_bid", "create_profile_boost", "manage_profile_boost"], "type": "string"}}
--- help ---
{"description":"Manage freelancer profile visibility boosters: availability badge (PIB) and profile boost ad. All write operations spend Connects and WRITE OPERATION — REQUIRES EXPLICIT USER CONFIRMATION.\n\nWRITE OPERATION — REQUIRES EXPLICIT USER CONFIRMATION.\nReturns a draft for review — call confirm_draft to execute after explicit user approval.\n\nActions:\n- get_status: Read current state of visibility boosters: availability badge (on/off, weekly Connects budget, cost) and profile boost ad (status, bid, targeting). No params required.\n- toggle_availability_badge: Enable or disable the Availability Badge (Available Now). Spending up to max_weekly_connects Connects per week when enabled. WRITE OPERATION — REQUIRES EXPLICIT USER CONFIRMATION. Params: enabled (bool, required), max_weekly_connects (int, required when enabling — maximum Connects to spend per week, e.g. 8).\n- get_suggested_boost_bid: Get the suggested Connects bid per click for a profile boost. Params: l1_category_id (string, required — L1 category ID), l3_category_id (string, required — L3 specialty ID).\n- create_profile_boost: Create a profile boost ad to appear at the top of search results. Charged only when a client clicks. WRITE OPERATION — REQUIRES EXPLICIT USER CONFIRMATION. Params: bid_connects (int, required — Connects per click), l1_category_id (string, required), l3_category_id (string, required), daily_budget (int, optional — max Connects/day), total_budget (int, optional — lifetime Connects cap), end_date (string, optional — yyyy-mm-dd).\n- manage_profile_boost: Stop, pause, restart, or update an existing profile boost ad. WRITE OPERATION — REQUIRES EXPLICIT USER CONFIRMATION. Params: action (string, required — one of: stop, pause, restart, update), ad_id (string, required), bid_connects (int, optional — for update), daily_budget (int, optional — for update), total_budget (int, optional — for update), end_date (string, optional — yyyy-mm-dd, for update).\n","input_schema":{"properties":{"action":{"description":"The operation to perform","enum":["get_status","toggle_availability_badge","get_suggested_boost_bid","create_profile_boost","manage_profile_boost"],"type":"string"},"org_uid":{"description":"Your Upwork org_uid from list_accounts","type":"string"},"params":{"description":"Action-specific parameters (see allOf branches per action when present)","type":"object"}},"required":["action","org_uid"],"type":"object"},"name":"boost_profile","read_only":false,"trace_id":"1d45018057f19b332b152c650a91bd0d"}

=== SCHEMA upwork__get_account ===
{"action": {"description": "The operation to perform", "enum": ["get_user_details", "get_organization", "get_company"], "type": "string"}, "org_uid": {"description": "Your Upwork org_uid from list_accounts", "type": "string"}, "params": {"description": "Action-specific parameters (see allOf branches per action when present)", "type": "object"}}
--- help ---
{"description":"View account info and organization details.\n\nActions:\n- get_user_details: Get user details. Params: id (string, optional — defaults to current user).\n- get_organization: Get current organization. No params.\n- get_company: Get the company details of the organization you are acting as. To read a different account of yours, call again with that org_uid. For a marketplace job's client, use find_jobs action=get with the job ID instead. No params.\n","input_schema":{"properties":{"action":{"description":"The operation to perform","enum":["get_user_details","get_organization","get_company"],"type":"string"},"org_uid":{"description":"Your Upwork org_uid from list_accounts","type":"string"},"params":{"description":"Action-specific parameters (see allOf branches per action when present)","type":"object"}},"required":["action","org_uid"],"type":"object"},"name":"get_account","read_only":true,"trace_id":"9c740f9f677cd7e3d94f581adb66a9a1"}

=== SCHEMA upwork__get_agency_dashboard ===
{"action": {"description": "The operation to perform", "enum": ["check"], "type": "string"}, "org_uid": {"description": "Your Upwork org_uid from list_accounts", "type": "string"}, "params": {"type": "object", "description": "Action-specific parameters (see allOf branches per action when present)"}}
--- help ---
{"description":"Get a consolidated overview of what's new: invitations, offers, messages, matching jobs, contracts, plus agency teams and members — all in one call. Access is scoped by your agency role.\n\nActions:\n- check: Check all activity sources in parallel. No params required.\n","input_schema":{"properties":{"action":{"description":"The operation to perform","enum":["check"],"type":"string"},"org_uid":{"description":"Your Upwork org_uid from list_accounts","type":"string"},"params":{"description":"Action-specific parameters (see allOf branches per action when present)","type":"object"}},"required":["action","org_uid"],"type":"object"},"name":"get_agency_dashboard","read_only":true,"trace_id":"9b0b76b8efe72ffa7e7a317ed096d19d"}

=== SCHEMA upwork__get_freelancer_dashboard ===
{"action": {"description": "The operation to perform", "enum": ["check"], "type": "string"}, "org_uid": {"description": "Your Upwork org_uid from list_accounts", "type": "string"}, "params": {"description": "Action-specific parameters (see allOf branches per action when present)", "type": "object"}}
--- help ---
{"description":"Get a consolidated overview of what's new: invitations, offers, messages, matching jobs, contract updates, and connects balance — all in one call.\n\nActions:\n- check: Check all activity sources in parallel. No params required.\n","input_schema":{"properties":{"action":{"description":"The operation to perform","enum":["check"],"type":"string"},"org_uid":{"description":"Your Upwork org_uid from list_accounts","type":"string"},"params":{"description":"Action-specific parameters (see allOf branches per action when present)","type":"object"}},"required":["action","org_uid"],"type":"object"},"name":"get_freelancer_dashboard","read_only":true,"trace_id":"a8ba2d96fcfcd7a0dc366c368999e7fd"}

=== SCHEMA upwork__list_accounts ===
{}
help err Unknown tool "list_accounts". Use search_tools to find valid tool names. (trace_id: 1245bf60628ab556a97396a8de158d6e)
help2 err Unknown tool "upwork__list_accounts". Use search_tools to find valid tool names. (trace_id: b98b4d3b273e0d165a207df7575a6653)

=== SCHEMA upwork__list_freelancer_proposals ===
{"action": {"description": "The operation to perform", "enum": ["list", "invitations", "get", "get_room"], "type": "string"}, "org_uid": {"description": "Your Upwork org_uid from list_accounts", "type": "string"}, "params": {"description": "Action-specific parameters (see allOf branches per action when present)", "type": "object"}}
--- help ---
{"description":"View your freelancer proposals and received invitations. Prior proposals are useful examples when drafting new proposals. Note: the status 'Accepted' means the proposal was submitted and validated (NOT that the client accepted you) — each result includes a status_label with the plain-English meaning. Start with recent submitted (Accepted) proposals; when looking for the strongest templates, also check Offered/Hired examples.\n\nActions:\n- list: List your proposals with status (plus status_label), bid terms, created/modified dates, job id/title, and cover letter text. Params: status (string, default Accepted — which means 'submitted'; values: Accepted=submitted, Offered=offer received, Hired, Activated=invitation accepted, Pending=processing, Declined, Withdrawn, Archived), sort_field (CREATEDDATETIME/MODIFIEDDATETIME, default CREATEDDATETIME), sort_order (ASC/DESC, default DESC), limit (integer, 1–10, default 10), cursor (string, optional — pageInfo.endCursor from a prior list for the next page; alias: after). Optional params are refinements: do not silently invent values. If the user makes a broad request, briefly surface the most relevant available refinements and proceed with only the required params plus context the user already provided. Ask before applying optional filters when the user asks for a selective result such as best, top, cheapest, near me, urgent, or only. After returning results, mention useful refinements the user can apply.\n- invitations: List invitations you received from clients. The 'id' field in each result is the invitation_uid needed for manage_proposals action=accept_invitation. Each result includes jobPosting (id + title); for full job details call find_jobs action=get with jobPosting.id. Params: status (string, default pending), job_posting_id (string, optional — only invitations for that job), sort_field (CREATED, default CREATED), sort_order (ASC/DESC, default ASC), limit (integer, 1–10, default 10), cursor (string, optional — pageInfo.endCursor from a prior list for the next page; alias: after). Optional params are refinements: do not silently invent values. If the user makes a broad request, briefly surface the most relevant available refinements and proceed with only the required params plus context the user already provided. Ask before applying optional filters when the user asks for a selective result such as best, top, cheapest, near me, urgent, or only. After returning results, mention useful refinements the user can apply.\n- get: Get full details of a proposal. Params: id or proposal_id (string, required — from list). The response may include insights: how many proposals the job has, how many the client opened, shortlisted or messaged, and what the competing applicants look like on average (earnings, completed jobs, years on Upwork, Job Success Score) plus their most common skills. Present them as the client's activity and the field so far, not as a verdict on the proposal. When insights_available is false, relay insights_note as written: never say the job has no other applicants, and never estimate the numbers yourself.\n- get_room: Get the messaging room for a proposal. Params: proposal_id (string, required). Freelancers (TALENT): cannot initiate a proposal room or send the first message on a proposal — the client must contact you first. To reply, use an existing room from list_freelancer_proposals action=get_room or get_messages action=find_room (context_type=proposal). If no room exists yet, tell the user the client needs to message first.\n","input_schema":{"allOf":[{"if":{"properties":{"action":{"const":"list"}},"required":["action"]},"then":{"properties":{"params":{"properties":{"after":{"description":"Alias for cursor","type":"string"},"cursor":{"description":"Page cursor (alias: after)","type":"string"},"limit":{"default":10,"description":"Max results (1–10, default 10)","maximum":10,"minimum":1,"type":"integer"},"sort_field":{"description":"CREATEDDATETIME or MODIFIEDDATETIME","type":"string"},"sort_order":{"description":"ASC or DESC","type":"string"},"status":{"description":"Proposal status filter (default Accepted)","type":"string"}},"type":"object"}}}},{"if":{"properties":{"action":{"const":"invitations"}},"required":["action"]},"then":{"properties":{"params":{"properties":{"after":{"description":"Alias for cursor","type":"string"},"cursor":{"description":"Page cursor (alias: after)","type":"string"},"job_posting_id":{"description":"Optional job filter","type":"string"},"limit":{"default":10,"description":"Max results (1–10, default 10)","maximum":10,"minimum":1,"type":"integer"},"sort_field":{"description":"Sort field (default CREATED)","type":"string"},"sort_order":{"description":"ASC or DESC","type":"string"},"status":{"description":"Invitation status (default pending)","type":"string"}},"type":"object"}}}},{"if":{"properties":{"action":{"const":"get"}},"required":["action"]},"then":{"properties":{"params":{"properties":{"id":{"description":"Proposal ID from list (preferred)","type":"string"},"proposal_id":{"description":"Alias for id — provide id or proposal_id","type":"string"}},"type":"object"}}}}],"properties":{"action":{"description":"The operation to perform","enum":["list","invitations","get","get_room"],"type":"string"},"org_uid":{"description":"Your Upwork org_uid from list_accounts","type":"string"},"params":{"description":"Action-specific parameters (see allOf branches per action when present)","type":"object"}},"required":["action","org_uid"],"type":"object"},"name":"list_freelancer_proposals","read_only":true,"trace_id":"2cc12c1b325e333575e2bd4dcf902465"}

=== SCHEMA upwork__manage_proposals ===
{"action": {"description": "The operation to perform", "enum": ["create", "accept_invitation", "decline_invitation", "withdraw", "edit_terms", "send_proposal", "acknowledge_policy"], "type": "string"}, "org_uid": {"description": "Your Upwork org_uid from list_accounts", "type": "string"}, "params": {"description": "Action-specific parameters \u2014 call get_tool_help(tool_name=\"manage_proposals\") for the full field list.", "type": "object"}}
--- help ---
{"description":"Submit, edit, or withdraw freelancer proposals. Also accept or decline client job invitations. For client offers, use respond_to_offer. Freelancers (TALENT): cannot initiate a proposal room or send the first message on a proposal — the client must contact you first. To reply, use an existing room from list_freelancer_proposals action=get_room or get_messages action=find_room (context_type=proposal). If no room exists yet, tell the user the client needs to message first.\n\nWRITE OPERATION — REQUIRES EXPLICIT USER CONFIRMATION.\nReturns a draft for review — call confirm_draft to execute after explicit user approval.\n\nActions:\n- create: Prepare a proposal draft for a marketplace job. EXISTING INVITE/PROPOSAL CHECK — MANDATORY, NEVER SKIP: before preparing a create draft you MUST call list_freelancer_proposals action=invitations to check whether the freelancer already has an invitation for this job, and action=list to check for an existing (pending, active, or declined) proposal. If an invitation exists, do NOT use create — use accept_invitation instead. Calling create when an invite or prior proposal already exists is rejected by Upwork (error VJ-JA-10) and wastes the user's turn. Returns a preview — the proposal is NOT submitted yet. Preview includes connects_cost (the connects REQUIRED to apply — the job's price, NOT the freelancer's balance), connects_balance (the freelancer's available connects), whether the freelancer can apply, and bid stats (avg/min/max rates from other proposals). bid_stats is a Freelancer Plus feature: when the account is not entitled it is replaced by bid_stats_available:false plus bid_stats_note — relay that note's framing and never estimate the amounts yourself. preview.plan is the account's plan name; mention it only when it explains a limitation the user ran into. Preview also includes a 'boost' block — boosting is a connects BID (not a flat fee) for a top slot on the client's list; Upwork charges it only if you finish in the top 4 or the client engages the proposal before the auction closes. If boost.available is false, do NOT offer boosting at all — it is unavailable (e.g. you do not meet the client's preferred qualifications; see boost.reason). Otherwise: boost.current_top_bids are the ACTUAL competing bids on this job (highest first; empty if no one has boosted) — present those, not any invented numbers. If boost.current_top_bids_available is false the bids could not be fetched — say the current bids are unknown; do NOT say no one has boosted. Offer boosting unless boost.recommendation is 'skip' (e.g. the client already hired). boost.recommended_connects is the smallest bid that secures a top slot (often just 1 connect when few or none have boosted) — note it but let the user pick. Never exceed boost.your_balance; apply ONLY the amount the user approves (pass it as boost_connects). If the preview contains unmet_preferred_qualifications, tell the user which of the client's preferred qualifications their proposal does not meet — this is advisory only and does NOT block submission (they can still submit). Present the full preview to the user and ask for confirmation. ALWAYS state connects_cost (the Connects this application will spend) and connects_balance explicitly when you present the preview — never omit the Connects cost. Offer to strengthen the proposal by attaching the freelancer's relevant portfolio projects and/or certificates: ASK the user whether they want to include any, call get_profile action=list_highlights to list what's available, help pick the most relevant to this job, and pass the chosen ids as certificate_ids / portfolio_project_ids. FILE ATTACHMENTS — MANDATORY, NEVER SKIP: before submitting, you MUST ask the user whether they want to attach any files to the proposal (e.g. a resume/CV, work samples, a brief). Never silently skip this question, even though the field is optional. If they say yes, call start_attachment_upload with context='proposals' FIRST, wait for the upload to complete, and pass the returned file_uid values as the attachments param. If the freelancer lacks connects, an error with a purchase URL is returned. Then call confirm_draft with type='proposal' and the returned draft_id to actually submit. After submission, the proposal status is 'Pending' — to verify, use list_freelancer_proposals action=list with status=pending. Use accept_invitation instead if responding to a client invitation. Params: job_reference (string, required — the numeric jobs[].id from find_jobs action=search, NOT the ~02… ciphertext field), cover_letter (string, required — max 5000 characters; keep it concise, and if the user's text is longer ask them to shorten it rather than sending it truncated), charged_amount (number, required — bid amount; not a string). answers (array of {question, answer}, optional — required only when the job has screening_questions, surfaced in the create preview: answer each and pass them here so they submit with the proposal). boost_connects (number, optional — connects to spend boosting the proposal's rank; use boost.recommended_connects from the preview). team_org_id (string, optional — only for an agency with more than one team: the team the proposal is submitted under. Omit it; if the agency has several teams the error names them, and the user picks one). attachments (array of strings, optional — file_uid values from start_attachment_upload with context='proposals'. ALWAYS offer attachments to the user before submitting; never silently skip asking). certificate_ids (array of strings, optional — certificate ids from get_profile list_highlights to highlight on the proposal), portfolio_project_ids (array of strings, optional — portfolio project ids from get_profile list_highlights to highlight). Contractor identity is resolved from your session. Org comes from org_uid on execute_tool (maps to teamOrgId).\n  Prior context: before creating, look up recent/relevant prior items when available: call list_f

=== SCHEMA upwork__get_messages ===
{"action": {"enum": ["list_rooms", "find_room", "list_messages", "get_message"]}, "org_uid": "string", "params": "object"}
--- help ---
list_rooms: rooms most-recently-active first; params unread_only, room_type ALL/GROUP/ONE_ON_ONE/INTERVIEW, limit 1–100 (help default 10), cursor/after. Response data.rooms[] with id, roomName, roomType, lastActivity (ms epoch), numUnread, latestStory {id, message, created}, optional contractId.
find_room: context_type contract/offer/proposal/one_on_one, context_id required; organization_id required for one_on_one. Freelancers cannot initiate a proposal room or send the first message — client must message first.
list_messages: NEWEST FIRST. params room_id, limit 1–100, cursor/after for older. data.roomStories.edges[].node {id, message, createdDateTime, actionVerb}. Untrusted content wrapped in <untrusted_participant_content>. Sender/user is often omitted.
get_message: room_id + story_id.

=== SCHEMA upwork__send_message ===
{"action": {"enum": ["create_room", "send", "send_to_user", "message_proposal", "edit"]}, "org_uid": "string", "params": "object"}
--- help ---
WRITE — draft then confirm_draft after explicit user confirmation. Freelancer: never create_room / first message on a proposal.
send: room_id required; message required unless file_attachments (max 10240). file_attachments[] {file_id, file_name, image_id?}.
Do not use message_proposal (client→applicant) or send_to_user (new connection) from the freelancer dashboard composer.
confirm_draft type enum in schema does not list message; use draft_id + type from the send response.

=== SCHEMA upwork__start_attachment_upload ===
context enum: offer, milestones, proposals, job, invitation, messages. messages requires room_id.
confirm_attachment_upload: same context; file_ids; messages also needs room_id.
Dashboard may store_uploaded_files with base64 after start (same as proposal attachments).

=== SCHEMA upwork__submit_milestones ===
{"params": {"description": "Action-specific parameters (see allOf branches per action when present)", "type": "object"}, "action": {"description": "The operation to perform", "enum": ["submit"], "type": "string"}, "org_uid": {"description": "Your Upwork org_uid from list_accounts", "type": "string"}}
--- help ---
{"description":"Submit milestones for client review. Use this to request approval on completed milestone work.\n\nWRITE OPERATION — REQUIRES EXPLICIT USER CONFIRMATION.\nReturns a draft for review — call confirm_draft to execute after explicit user approval.\n\nActions:\n- submit: Submit a milestone for client review/approval. Params: milestone_id (string, required), message (string, required — note to the client), amount (object, optional — { decimal_amount: string, currency: string } to submit a partial amount), attachments (array of strings, optional — file uids to attach as deliverables; upload them first with the 'milestones' upload context).\n","input_schema":{"properties":{"action":{"description":"The operation to perform","enum":["submit"],"type":"string"},"org_uid":{"description":"Your Upwork org_uid from list_accounts","type":"string"},"params":{"description":"Action-specific parameters (see allOf branches per action when present)","type":"object"}},"required":["action","org_uid"],"type":"object"},"name":"submit_milestones","read_only":false,"trace_id":"20a497c615770cc0311a201fea718727"}